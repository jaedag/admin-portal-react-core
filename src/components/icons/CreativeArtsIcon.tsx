import React from 'react'
import { IconBaseProps } from 'react-icons'
import { BsMusicNoteBeamed } from 'react-icons/bs'

const CreativeArtsIcon = ({ ...rest }: IconBaseProps) => {
  return <BsMusicNoteBeamed color="white" {...rest} />
}

export default CreativeArtsIcon
